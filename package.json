{
  "name": "lonaat-backend-wrapper",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd .. && cd backend-node && npm ci && npm run build",
    "start": "cd backend-node && npm start"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@radix-ui/react-dialog": "^1.1.15",
    "@radix-ui/react-select": "^2.2.6",
    "axios": "^1.13.2",
    "fast-xml-parser": "^5.3.3",
    "leaflet": "^1.9.4",
    "morgan": "^1.10.1",
    "react-leaflet": "^5.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "jsdom": "^28.1.0",
    "vitest": "^4.0.18"
  }
}
